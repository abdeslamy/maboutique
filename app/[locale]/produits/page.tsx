import CatalogueClient from "@/components/CatalogueClient";

// Page Catalogue (/produits) : tout le travail est dans CatalogueClient
// (recherche + filtre + grille). On garde la page très fine.
export default function PageProduits() {
  return <CatalogueClient />;
}
